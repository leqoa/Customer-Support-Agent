# Runtime dependencies only. Dev/test/lint tooling lives in requirements-dev.txt.
# NOTE: `logging` was previously (incorrectly) pinned here as `logging==0.5.1.2`.
# `logging` is part of the Python standard library, not a PyPI package to install;
# the PyPI package of that name is an unrelated, ancient artifact that fails to
# build on modern Python. It has been removed.
fastapi==0.104.0
uvicorn==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
requests==2.31.0
aiohttp==3.9.1
PyYAML==6.0.1
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.27.2  # required by fastapi.testclient.TestClient (Starlette 0.27.x TestClient needs httpx<0.28)

# Database / persistence layer (backend/models/db/)
sqlalchemy==2.0.23
alembic==1.13.0
psycopg2-binary==2.9.9  # Postgres driver for production (config/settings.yaml database.type: postgresql)
# Local dev needs no extra driver: DATABASE_URL defaults to a SQLite file
# (sqlite:///./itss_dev.db), which SQLAlchemy supports natively.
