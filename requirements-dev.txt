# Dev/test/lint tooling — not required for a production deploy.
# Install alongside requirements.txt for local development and CI:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==7.4.3
pytest-asyncio==0.21.1
ruff==0.1.6
